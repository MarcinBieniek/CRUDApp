const ButtonEditPost = props => {

    return (
        <button 
        type="button" 
        className="btn btn-outline-info h-100">
        {props.children}</button>
    );
};

export default ButtonEditPost
